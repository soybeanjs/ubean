#!/usr/bin/env node
// @ubean/cli CLI binary (temporary name: ubean-next, will be renamed to ubean in stage 7)
// Imports the CLI entry which calls `runMain(main)` at the top level.
import '../dist/cli.mjs';
