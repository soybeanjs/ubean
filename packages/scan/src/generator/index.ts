/**
 * BC re-export —— 实体路由文件生成器已下沉到页面路由所有者 `@ubean/vue`
 * (`@ubean/vue/generator` 子路径)。本文件仅保持 `@ubean/scan/generator`
 * 导入路径兼容;新代码请直接从 `@ubean/vue/generator` 导入。
 */
export { generateRouteFiles, RouteFileGenerator, DEFAULT_HEADER_COMMENT } from '@ubean/vue/generator';
export type { GeneratorOptions, GeneratorResult, GeneratorScanInput } from '@ubean/vue/generator';
