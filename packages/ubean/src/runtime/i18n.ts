/**
 * ubean/runtime/i18n — 服务端 i18n 入口
 *
 * 纯 re-export `@ubean/i18n`: ALS `t()`/`d()`/`n()`、`compileLocalePaths`、
 * `createI18nMiddleware`、`runWithI18n`。
 *
 * 浏览器端 Vue 版本从 `ubean/runtime/vue` 导入(`useI18n` / `setLocale`)。
 */
export * from '@ubean/i18n';
