/**
 * ubean/runtime/vue — Vue 客户端运行时(遗留子路径)
 *
 * 向后兼容:转发到 `@ubean/runtime`。
 *
 * 此入口同时通过 `declare module 'vue-router'` 扩展 `RouteMeta`,
 * 与 `@ubean/runtime` 主入口保持一致。
 */
export * from '@ubean/runtime';
