/**
 * ubean/vue-ssr — Vue SSR 渲染器入口
 *
 * 纯 re-export `@ubean/ssr`,提供 `createVueRenderer` 等 SSR 渲染 API。
 *
 * ```ts
 * import { createVueRenderer } from 'ubean/vue-ssr';
 * ```
 *
 * 对应原 ubean 包 `core/vue/renderer.ts` 的行为。
 */
export * from '@ubean/ssr';
